services:
  - type: web
    name: growth-cloud-backend
    env: node
    rootDir: backend
    buildCommand: npm run build
    startCommand: npm run dev
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
